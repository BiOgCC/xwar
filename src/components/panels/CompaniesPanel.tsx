import CompaniesTab from './CompaniesTab'

// CompaniesTab already contains the full companies UI.
// This panel just renders it directly as a standalone panel.
export default function CompaniesPanel() {
  return <CompaniesTab />
}
